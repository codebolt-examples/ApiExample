{
  "name": "math-api",
  "version": "1.0.0",
  "description": "A math API using Cloudflare Workers and Hono",
  "main": "src/index.ts",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^3.9.0",
    "hono": "^2.3.3"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "esbuild": "^0.12.28"
  }
}